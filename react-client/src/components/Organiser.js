/*import React, { Component } from 'react'
import {
  createFragmentContainer,
  graphql
} from 'react-relay'

export class Organiser extends Component {
  render() {
    return (
      <div>
        { this.props.organiser.name } - { this.props.organiser.slug }
      </div>
    )
  }
}

export const OrganiserContainer = createFragmentContainer(Organiser, graphql`
  fragment Organiser_organiser on Organiser {
    id
    name
    slug
  }
`)*/
